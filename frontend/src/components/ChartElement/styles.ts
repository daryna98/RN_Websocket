import { StyleSheet } from "react-native";

export default StyleSheet.create({
  chartContainer: {
    marginTop: 20,
  },
  chartRow: {
    display: "flex",
    flexDirection: "row",
    alignItems: "center",
  },
  chartDate: {
    width: 400,
    paddingLeft: 50,
    fontSize: 12,
  },
  chartNumber: {
    width: 50,
    fontSize: 16,
  },
});
